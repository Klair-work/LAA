title: Layered Agent Architecture
name: Layered Agent Architecture
description: A whitepaper on composable, governed AI workers and trust-centered orchestration.
url: https://whitepaper.klair.work
baseurl: ""

plugins:
  - jekyll-feed
  - jekyll-seo-tag

markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge

exclude:
  - Gemfile
  - Gemfile.lock
  - vendor
